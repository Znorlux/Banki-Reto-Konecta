import { Dashboard } from "@/components/Dashboard";

// Este enfoque mantiene todas las "páginas" en la carpeta pages,
// pero delega toda la implementación a los componentes reutilizables
const DashboardPage = () => {
  return <Dashboard />;
};

export default DashboardPage;
